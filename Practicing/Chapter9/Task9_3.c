#include <stdio.h>

int main () {

    int *pc, *pi, *pd;

    printf("pc = %p, pc + 1 = %p, pc + 2 = %p\n\n", pc, pc + 1, pc + 2);
    printf("pi = %p, pi + 1 = %p, pi + 2 = %p\n\n", pi, pi + 1, pi + 2);
    printf("pd = %p, pd + 1 = %p, pd + 2 = %p\n\n", pd, pd + 1, pd + 2);


    return 0;
}